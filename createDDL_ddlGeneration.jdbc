CREATE TABLE USERDETAIL (ID INTEGER NOT NULL, NAME VARCHAR(255), PASSWORD VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE TODO (ID BIGINT AUTO_INCREMENT NOT NULL, DESCRIPTION VARCHAR(255), SUMMARY VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PHONEBOOK (PHONENUM VARCHAR(255) NOT NULL, NAME VARCHAR(255), PRIMARY KEY (PHONENUM))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
